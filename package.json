{
  "name": "codetest-taxonomy",
  "version": "1.0.0",
  "description": "One challenge we have at Littledata is to categorise our client's websites by industry sector, so we can compare them with websites from the same industry.",
  "scripts": {
    "install-packages": "npm install && cd ./frontend && npm install && cd ../backend && npm install && cd ../",
    "start": "pm2 start ecosystem.config.js",
    "stop": "pm2 stop ecosystem.config.js",
    "api-test": "cd backend/ && npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oleg-kozakgroup/codetest-taxonomy.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/oleg-kozakgroup/codetest-taxonomy/issues"
  },
  "homepage": "https://github.com/oleg-kozakgroup/codetest-taxonomy#readme",
  "dependencies": {
    "pm2": "^3.2.4"
  }
}
